services:
  user-service:
    image: userservice
    build:
      context: .
      dockerfile: Dockerfile
    container_name: user-service
    networks:
      - netw
    ports:
      - "${USER_SERVER_PORT}:${USER_CONTAINER_PORT}"
    env_file:
      - "ENV=PROD"
      - .env
networks:
  netw:
    driver: bridge