services:
  api-gateway:
    build:
      dockerfile: Dockerfile
    image: apigateway
    container_name: api-gateway
    networks:
      - netw
    ports:
      - "${API_GATEWAY_SERVER_PORT}:${API_GATEWAY_CONTAINER_PORT}"
    env_file:
      - .env

networks:
  netw:
    driver: bridge